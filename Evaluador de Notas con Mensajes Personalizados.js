// Declaración de la variable nota
let nota = 85; // Puedes cambiar este valor para probar diferentes casos

// Verificación si la nota es válida
if (nota >= 0 && nota <= 100) {
    if (nota >= 90) {
        console.log(`Tu nota es ${nota}. ¡Excelente, aprobaste!`);
    } else if (nota >= 75) {
        console.log(`Tu nota es ${nota}. ¡Bien hecho, aprobaste!`);
    } else if (nota >= 60) {
        console.log(`Tu nota es ${nota}. Aprobaste con lo justo.`);
    } else {
        console.log(`Tu nota es ${nota}. No aprobaste, debes esforzarte más.`);
    }
} else {
    console.log("Por favor ingresa una nota válida entre 0 y 100.");
}

   nota = 75;

// Verificación si la nota es válida
if (nota >= 0 && nota <= 100) {
    if (nota >= 90) {
        console.log(`Tu nota es ${nota}. ¡Excelente, aprobaste!`);
    } else if (nota >= 75) {
        console.log(`Tu nota es ${nota}. ¡Bien hecho, aprobaste!`);
    } else if (nota >= 60) {
        console.log(`Tu nota es ${nota}. Aprobaste con lo justo.`);
    } else {
        console.log(`Tu nota es ${nota}. No aprobaste, debes esforzarte más.`);
    }
} else {
    console.log("Por favor ingresa una nota válida entre 0 y 100.");
}

nota = 65;

// Verificación si la nota es válida
if (nota >= 0 && nota <= 100) {
    if (nota >= 90) {
        console.log(`Tu nota es ${nota}. ¡Excelente, aprobaste!`);
    } else if (nota >= 75) {
        console.log(`Tu nota es ${nota}. ¡Bien hecho, aprobaste!`);
    } else if (nota >= 60) {
        console.log(`Tu nota es ${nota}. Aprobaste con lo justo.`);
    } else {
        console.log(`Tu nota es ${nota}. No aprobaste, debes esforzarte más.`);
    }
} else {
    console.log("Por favor ingresa una nota válida entre 0 y 100.");
}

nota = 55;

// Verificación si la nota es válida
if (nota >= 0 && nota <= 100) {
    if (nota >= 90) {
        console.log(`Tu nota es ${nota}. ¡Excelente, aprobaste!`);
    } else if (nota >= 75) {
        console.log(`Tu nota es ${nota}. ¡Bien hecho, aprobaste!`);
    } else if (nota >= 60) {
        console.log(`Tu nota es ${nota}. Aprobaste con lo justo.`);
    } else {
        console.log(`Tu nota es ${nota}. No aprobaste, debes esforzarte más.`);
    }
} else {
    console.log("Por favor ingresa una nota válida entre 0 y 100.");
}